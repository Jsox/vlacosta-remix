// import * as dotenv from 'dotenv';
// dotenv.config();
class api {
    // constructor() {}

    pe(): number {
        let a: number = 2;
        return a;
    }
}

const Api = new api();
export default Api;
